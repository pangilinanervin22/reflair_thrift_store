import NotFoundContent from "@/components/NotFound/NotFoundContent";

// notFound() thrown inside a shop page lands here, inside the shop layout.
export default function ShopNotFound() {
    return <NotFoundContent />;
}
